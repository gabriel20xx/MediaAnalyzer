# Path on your HOST machine that contains media files
# Windows example:
# MEDIA_HOST_PATH=C:\Users\<you>\Videos
MEDIA_HOST_PATH=

# Host port for the web UI (defaults to 3000)
# If 3000 is already used, try 3001.
HOST_PORT=

# Postgres settings (optional overrides)
POSTGRES_USER=media
POSTGRES_PASSWORD=media
POSTGRES_DB=media
POSTGRES_HOST_PORT=5432

# Optional: override the app's DB connection string directly.
# Format:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=disable
# Examples:
# - Inside docker-compose (default): postgresql://media:media@postgres:5432/media?sslmode=disable
# - From host tools:              postgresql://media:media@localhost:5432/media?sslmode=disable
DATABASE_URL=

# Startup retry knobs for DB initialization
DB_INIT_MAX_ATTEMPTS=30
DB_INIT_DELAY_MS=500

# Auto-analyze watcher
# WATCH_MEDIA=1 enables watching MEDIA_ROOT for new/changed files.
WATCH_MEDIA=1
# Debounce (and write-stability) delay before analyzing a new file
WATCH_DEBOUNCE_MS=2000

# Analyze-all batching (lower if you see timeouts or high CPU)
ANALYZE_ALL_BATCH_SIZE=25
